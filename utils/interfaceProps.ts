export interface StateProps {
  isDarkMode: boolean;
}
export interface dataAppartementProps {
  id: number;
  name: string;
  description: string;
  price: number;
  picture: string;
  surface: number;
  adress: string;
}
